<%=@error%>
<%= @task.title %>

<%= @task.complete %>
<% if current_user.id == @task.user_id %>
<%= link_to "Edit this Task", edit_task_path(@task.id)%>
<%= button_to "Delete", task_path(@task), method: 'delete',  class: "button-small btn"%>
<% end %>